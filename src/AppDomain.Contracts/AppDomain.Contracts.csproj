<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>True</IsPackable>
        <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
        <VersionSuffix Condition="'$(Configuration)' == 'Debug'">pre</VersionSuffix>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>

    <PropertyGroup>
        <PackageTags>AppDomain.contracts</PackageTags>
        <IncludeSource>true</IncludeSource>
        <Authors>OrgName</Authors>
        <Company>OrgName</Company>
        <Copyright>Copyright (c) OrgName. All rights reserved.</Copyright>
        <RepositoryUrl>https://github.com/OrgName/AppDomain</RepositoryUrl>
        <RepositoryType>git</RepositoryType>
    </PropertyGroup>

    <ItemGroup>
        <AssemblyAttribute Include="Momentum.Extensions.Abstractions.Messaging.DefaultDomainAttribute">
            <Domain>AppDomain</Domain>
        </AssemblyAttribute>
    </ItemGroup>

    <ItemGroup>
        <Compile Include="..\AppDomain\**\Contracts\IntegrationEvents\*.cs" Link="%(RecursiveDir)%(Filename)%(Extension)"/>
        <Compile Include="..\AppDomain\**\Contracts\Models\*.cs" Link="%(RecursiveDir)%(Filename)%(Extension)"/>
        <!--#if (includeApi) -->
        <Content Include="..\AppDomain.Api\**\Protos\**\*.proto" Link="%(RecursiveDir)%(Filename)%(Extension)" Pack="true">
            <PackagePath>Protos</PackagePath>
        </Content>
        <!--#endif -->
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="Momentum.Extensions.Abstractions"/>
    </ItemGroup>

</Project>
